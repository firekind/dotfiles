#!/bin/bash

if [ "$1" == "-m" ]
then
	sudo mount -t cifs //192.168.12.20/CE /run/media/ouroboros/12.20 -o username=ce,password=sas,vers=1.0
elif [ "$1" == "-u" ]
then
	sudo umount /run/media/ouroboros/12.20
else
	echo "bad usage"
fi
